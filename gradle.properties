org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Pinned to match TrapPack 1.0.16 exactly (see ../modpacks/server.mrpack).
# Bumping any of these means rebuilding the pack, not just the jar.
minecraft_version=1.21.8
yarn_mappings=1.21.8+build.1
loader_version=0.19.3

mod_version=0.2.0
maven_group=dev.heezq
archives_base_name=trapcraft

fabric_version=0.136.1+1.21.8
# ponytail: compile-only. polymer-bundled is already in the pack, so this is
# NOT jar-in-jar'd -- shipping our own copy would load Polymer twice.
polymer_version=0.13.13+1.21.8
